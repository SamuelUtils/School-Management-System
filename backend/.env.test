PORT=3001
# For testing (optional, can be overridden in test setup)
DATABASE_URL="postgresql://postgres:SRHno4-87@localhost:5432/school_db_test?schema=public"

# New variables
JWT_SECRET="SchoolAppJWT" # Change this in your actual .env
JWT_EXPIRES_IN=1000000 # Example: 1 hour

# For OTP (optional, otplib has defaults but good to be explicit)
OTP_SECRET="SchoolAppJWT_FOR_HOTP" # Change this
OTP_WINDOW=1 # For TOTP, number of windows to check before and after current
OTP_STEP=2 # For TOTP, step in seconds (e.g., 5 minutes)
